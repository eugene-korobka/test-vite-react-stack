{
  "name": "server-fastify",
  "private": true,
  "version": "0.0.0",
  "description": "Backend on Fastify",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "fastify": "tsc -p tsconfig.json && node build/server.js"
  },
  "dependencies": {
    "@fastify/cors": "^8.2.0",
    "@fastify/mongodb": "^6.2.0",
    "fastify": "^4.14.1",
    "fastify-plugin": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^18.15.3",
    "typescript": "^4.9.3"
  },
  "license": "ISC"
}
